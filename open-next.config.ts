import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig({
  // Cloudflare-specific configuration for Atlas Dashboard
  // See https://opennext.js.org/cloudflare for more details
});
